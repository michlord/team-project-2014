#ifndef RENDERINGMANAGER_H_INCLUDED
#define RENDERINGMANAGER_H_INCLUDED

class RenderingManager {
    public:
        RenderingManager();
        ~RenderingManager();

        void render();

    private:

};

#endif